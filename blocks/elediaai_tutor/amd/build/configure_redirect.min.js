/**
 * Send this block's "Configure" action to the Plugin Shell instead of Moodle's
 * block-config modal. In Moodle 4.4+ the block "Configure" control opens a
 * dynamic-form modal (core_block/edit, a bubble-phase click listener on
 * `[data-action="editblock"][data-blockform]`). We rewrite our own controls into
 * plain links to the shell and, for controls inserted later via AJAX, intercept the
 * click in the capture phase (which runs before core's listener) and navigate there.
 *
 * @module     block_elediaai_tutor/configure_redirect
 * @copyright  2026 eLeDia GmbH, Berlin
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("block_elediaai_tutor/configure_redirect",[],(function(){const SELECTOR='[data-action="editblock"][data-blockform]',isOurControl=function(el){return Boolean(el)&&-1!==(el.getAttribute("data-blockform")||"").indexOf("elediaai_tutor")},targetFor=function(config,blockid){return blockid?config.instanceUrl.replace("__ID__",encodeURIComponent(blockid)):null};return{init:function(config){config=config||{},Array.prototype.forEach.call(document.querySelectorAll(SELECTOR),(function(el){if(!isOurControl(el))return;const url=targetFor(config,el.getAttribute("data-blockid"));url&&(el.removeAttribute("data-action"),el.setAttribute("href",url))})),document.addEventListener("click",(function(event){const el=event.target.closest?event.target.closest(SELECTOR):null;if(!isOurControl(el))return;const url=targetFor(config,el.getAttribute("data-blockid"));url&&(event.preventDefault(),event.stopImmediatePropagation(),window.location.assign(url))}),!0)}}}));

//# sourceMappingURL=configure_redirect.min.js.map